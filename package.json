{
  "name": "blog",
  "version": "1.0.0",
  "description": "learn code nodejs at f8",
  "main": "src/index.js",
  "scripts": {
    "beautiful": "lint-staged",
    "start": "nodemon --inspect src/index.js",
    "watch": "node-sass --watch src/resources/scss/ --output src/public/css/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "blog",
    "learning",
    "code",
    "f8"
  ],
  "author": "hieudev",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.2",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "lint-staged": {
    "src/**/*.{js,html}": "prettier --single-quote --trailing-comma all --tab-width 4 --write"
  },
  "devDependencies": {
    "express-handlebars": "^6.0.6",
    "lint-staged": "^13.1.0",
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.1"
  }
}
